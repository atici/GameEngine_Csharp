public static class Physics
{
	public const float GRAVITY = 9.98f;

	// public void Move(ref Transform transform, Vector2 offset)
	// {
	// 	Move(offset.X, offset.Y);
	// }

	// public void Move(int x, int y)
	// {
	// 	rect.X += x;
	// 	rect.Y += y;
	// }

	// public void CalculateGravity()
	// {
	// 	if ( is_static) return;
	// 	Move(0, GRAVITY);
	// }
}